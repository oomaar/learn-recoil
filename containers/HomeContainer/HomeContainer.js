import { Header } from "../../components";
import {
    Container,
} from "./styledHomeContainer";

export const HomeContainer = () => {
    return (
        <Container>
            <Header />
        </Container>
    );
};